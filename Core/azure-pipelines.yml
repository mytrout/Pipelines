name: 'Pipelines.Core v$(major).$(minor).$(patch).$(buildId)$(suffix)'

trigger:
  branches:
    include:
    - master
  paths:
    include:
    - Core/*
    exclude:
    - Steps/Core/*
    - README.md
    - License.md

pr:
  branches:
    include:
    - master
  paths:
    include:
    - Core/*
    exclude:
    - Steps/Core/*
    - README.md
    - License.md

pool:
  vmImage: 'windows-latest'

variables:
- name: 'solution'
  value: 'Core/*.sln'

- name: 'buildPlatform'
  value: 'Any CPU'

- name: 'buildConfiguration'
  value: 'Release'

- group: 'MyTrout.Pipelines.Version'

- name: 'buildId'
  value: $[counter(variables['patch'], 0)]

- group: 'Pipelines Artifacts Feed'
- group: 'SonarQube Analysis'

- name: 'sonarCloudProjectKey'
  value: 'Pipelines.Core'

- name: 'sonarCloudProjectName'
  value: 'Pipelines.Core'

- name: 'sonarCloudSourceBranch'
  value: 'refs/heads/master'
  
steps:
  - template: ../nuget-build-template.yml
